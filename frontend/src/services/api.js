import React from "react";
import Axios from "axios";

const getToken = async () => {
  // const jwt = await storage
}

const apiCall = async ({
  url = "",
  payload,
  method = "GET",
  headers = {},
  options = {},
  withAuth = true,
}) => {

  let token = null;
}